<%= form_for @photo, html: { multipart: true, class:"form-horizontal" } do |f| %>
  <% if @photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo.errors.count, "error") %> prohibited this leg from being saved:</h2>

      <ul>
      <% @photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= hidden_field_tag :leg_id, params[:leg_id] %>

  <div class="form-group">
    <%= f.label :title, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :title, {class: "form-control"} %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :lat, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :lat, {class: "form-control"} %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :lon, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :lon, {class: "form-control"} %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit nil, {class: "btn btn-primary"} %>
    </div>
  </div>
<% end %>
